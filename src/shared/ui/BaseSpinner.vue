<script setup lang="ts">
withDefaults(defineProps<{ size?: 'sm' | 'md' | 'lg'; label?: string }>(), { size: 'md' })
const map = { sm: 'h-4 w-4', md: 'h-6 w-6', lg: 'h-10 w-10' }
</script>

<template>
  <div class="inline-flex items-center gap-2 text-primary-600" role="status">
    <svg :class="['animate-spin', map[size]]" viewBox="0 0 24 24" fill="none">
      <circle class="opacity-20" cx="12" cy="12" r="10" stroke="currentColor" stroke-width="4" />
      <path class="opacity-80" fill="currentColor" d="M4 12a8 8 0 018-8v4a4 4 0 00-4 4H4z" />
    </svg>
    <span v-if="label" class="text-sm text-slate-500">{{ label }}</span>
  </div>
</template>
